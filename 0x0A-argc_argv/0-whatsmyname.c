#include "main.h"
#include <stdio.h>
/**
 * main - the main function
 * @argc: the arg counter
 * @argv: the arg vector
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	{
		printf("%s\n", *argv);
	}
	return (0);
}
